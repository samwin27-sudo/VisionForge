# Annotation Workflow

Draw boxes by dragging on the canvas. AI labels are `pending`; manual labels are `accepted`. Use Space to accept and X to reject selected predictions. Export accepted labels only for training.
