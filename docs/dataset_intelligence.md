# Dataset Intelligence

The analyzer calculates total images, annotated/unannotated images, class counts, source/status counts, object size distribution, class imbalance, segmentation coverage, file health, and split distribution.
