# Getting Started

Install with `pip install -r requirements.txt` and run `python run.py`. Open a local image folder, add classes, draw boxes, save the project, analyze, export, and generate a report.
