# YOLO Auto-Labeling

Install optional dependencies with `pip install -r requirements-ai.txt`. Click Auto Label, choose a `.pt` model, set confidence/IoU/image size/device, and run on current image or full dataset. Predictions enter review as pending.
