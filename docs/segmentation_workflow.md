# Segmentation Workflow

Install with `pip install -r requirements-segmentation.txt`. The backend supports SAM-compatible box-prompted masks through `SamSegmenter`. Use masks to generate polygons/bboxes and export COCO segmentation. v1 includes backend and export support; direct polygon point editing is on the roadmap.
